# Sermon Keeper Landing

Marketing website for the Sermon Keeper iOS app.

## Stack

- **Astro** static site hosted on **Vercel**. `npm run dev`, `npm run build`.
- Domain: **sermonkeeper.app**
- `build.format: 'directory'` + `trailingSlash: 'never'` emit `/blog/<slug>/index.html`,
  which preserves the URLs the old plain-HTML site served through `cleanUrls`.
  Do not change either setting: every blog URL depends on them.

## Structure

```
src/content/blog/<slug>.mdx   → one article each; the filename is the URL slug
src/content.config.ts         → frontmatter schema (Zod). NOT src/content/config.ts,
                                which Astro 7 rejects
src/layouts/BlogPost.astro    → article shell: JSON-LD, hero, credit line, rail, FAQ, CTA
src/pages/blog/[...slug].astro→ routes the collection
src/pages/                    → index, blog/index, about, privacy, terms, 404
src/components/               → Header, Footer, StickyBar, FAQAccordion, ArticleCTA,
                                ArticleCTACard, ArticleBanner, ArticleSidebar, RelatedPosts
src/styles/global.css         → all styles
src/utils/site.ts             → SITE, AUTHORS, appStoreUrl(token, placement)
public/                       → served verbatim: scss/image/, downloads/, robots.txt
```

Sitemap is generated by `@astrojs/sitemap` as `/sitemap-index.xml`; `vercel.json`
rewrites `/sitemap.xml` to it so the URL registered in Search Console still works.

## Blog

12 articles. Everything above the prose is frontmatter, not markup: title/meta,
dates, author, hero, `utmToken`, `faq[]`, `howTo`, `finalCta`, and exactly three
`related` slugs. An unknown related slug fails the build on purpose.

Article layout on wide screens is a 727px text column, a 79px gutter and a 307px
rail, centred as one block so the h1, hero image and body share a left edge.
Below 1220px the rail is hidden.

Never hand-write a FAQ section in the body: the layout builds one from `faq[]`.
Four articles once carried both and rendered the section twice.

## Key CSS classes

- `.article-layout` — the column + rail grid; `--col`, `--gap`, `--rail`, `--stack`
- `.article-content` — the 727px text column
- `.article-rail` / `.rail-card` — the rail and its cards, `border-radius: 32px`
- `.article-table` — comparison tables, `border-radius: 16px`, `border-collapse: separate`
- `.article-cta`, `.article-cta-card` — CTA blocks, `border-radius: 20px`
- `.article-related` — the "Keep reading" grid, spans the full `--stack`

## SEO

- Google Search Console verified (tag on all pages)
- Google Analytics: `G-M8FJDVHNCY`
- Sitemap is generated at build; no `lastmod` to maintain by hand
- Blog images from Unsplash (free commercial use)
- Structured data (JSON-LD) on all pages, built in `src/utils/seo.ts`
- Every App Store link goes through `appStoreUrl(utmToken, placement)`. Placement
  names are attribution history: renaming one splits a campaign's data.

## Writing style

House rules for every article, existing and new. Follow them when drafting and
when editing.

**Density is the main one. One idea = one paragraph, developed.**

A paragraph should carry a thought from start to finish: typically 45–90 words,
three to five sentences. State the point, then support it, then land it. Do not
chop a single thought into a run of one-sentence paragraphs — that is what makes
a page feel like debris rather than an argument, and it is the most common fault
in these articles.

Reference measurement (2026-07-05): our median paragraph was 30 words with 59% of
paragraphs under 35 words. The publications we are matching run 45–90. Never
"improve readability" by splitting a paragraph that holds one idea; a long
paragraph is only a problem when it holds two.

**Lists are for genuinely parallel, enumerable things** — steps in order,
options being compared, specs. If the items are full thoughts that build on each
other, they are prose and belong in a paragraph. A bulleted list of five
sentences reads worse than one good paragraph.

**One-sentence paragraphs are a tool for emphasis.** Use one deliberately, after
a developed paragraph, to land a point. A page with many of them has none.

Also:

- No parentheses in titles or headings. Say the qualifier plainly instead:
  "How to Record a Sermon on iPhone in 2026", not "(2026 Guide)"; "Method 1:
  Voice Memos, free and already on your phone", not "(free, already on your
  phone)".
- No em dashes. Use a colon when a label introduces what follows, a comma for an
  appositive, or a full stop when the second half stands on its own.
- **Bold only ever opens a block, never sits inside a sentence.** A run-in label
  at the very start of a paragraph or list item is fine — "Clear a little
  storage first.", "Step 1: Start talking about it". Emphasising a word or
  phrase mid-sentence is not: no bolding "an **original work of authorship**",
  no bolding a product name at the end of a recommendation. If a phrase needs
  weight, rewrite the sentence so it lands on its own. Run-in labels are set in
  Inter against the Charter body by CSS; just write them as `<strong>`.
- Every article opens with a lead paragraph in `<p class="article-intro">` that
  answers the query in its first sentence.
- Free tier wording is "Free 3-day trial" with no recording-count qualifier.

## Conventions

- Blog hero images: 1200px wide JPG, stored in `scss/image/blog/photos/`
- Image naming matches article slug: `<slug>.jpg`
- All new articles must be added to `sitemap.xml` and `blog/index.html`
- After changes: resubmit sitemap in Google Search Console
